.vscode/

terraform.tfplan
terraform.tfstate
./*.tfstate
*.terraform.lock.hcl
.terraform/
*.backup
tfplugindocs

ai/tasks

# Local toolchain pin, managed per-machine — version of record is go.mod's `go` line.
mise.toml
